{
  "name": "latios-core",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "dev:api": "pnpm --filter @latios/api start:dev",
    "dev:web": "pnpm --filter @latios/web dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  }
}
